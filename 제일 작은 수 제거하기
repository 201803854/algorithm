def solution(arr):
    min_val = min(arr)  # 리스트 arr에서 최솟값을 찾습니다.
    answer = list(filter(lambda x : x > min_val, arr))  # arr에서 min_val보다 큰 요소만 추출합니다.
    if answer == [] :
        answer = [-1] # 빈 배열일 경우 -1을 입력합니다.
    return answer  # 추출한 요소들을 반환합니다.