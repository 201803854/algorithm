def solution(n):
    i= 1  # 제곱근을 계산하기 위해 시작할 숫자를 1로 설정합니다.
    while i*i <= n :  # i의 제곱이 n보다 작거나 같은 동안 반복합니다.
        if i*i == n :  # 만약 i의 제곱이 n과 같다면,
            return (i+1)*(i+1)  # i에 1을 더하고 제곱한 값을 반환합니다.
        i+=1  # i의 값을 1 증가시킵니다.
    return