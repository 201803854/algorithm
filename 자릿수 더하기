def solution(n):
    answer = 0  # 각 자릿수의 합을 저장할 변수를 0으로 초기화합니다.
    temp = list(str(n))  # 입력받은 숫자 n을 문자열로 변환하고, 그 문자열을 리스트로 만듭니다.
    

    for i in temp:  # 숫자의 각 자릿수를 순회합니다.
        answer += int(i)  # 각 자릿수를 정수로 변환하고, 그 값을 answer에 더합니다.
        # 이렇게 하면, 숫자의 각 자릿수의 합을 구할 수 있습니다.

    return answer  # 숫자의 각 자릿수의 합을 반환합니다.